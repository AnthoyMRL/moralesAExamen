<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="moralesAExamen.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:moralesAExamen.Views"
    Title="MovieCatalogApp">

    <TabBar>
        <ShellContent
            Title="Agregar"
            ContentTemplate="{DataTemplate local:AddMoviePage}"
            Icon="add.png" />

        <ShellContent
            Title="Películas"
            ContentTemplate="{DataTemplate local:MovieListPage}"
            Icon="list.png" />

        <ShellContent
            Title="Logs"
            ContentTemplate="{DataTemplate local:LogsPage}"
            Icon="document.png" />
    </TabBar>

</Shell>